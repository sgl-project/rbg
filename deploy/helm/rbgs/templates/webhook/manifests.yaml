---
# ValidatingWebhookConfiguration for RoleBasedGroup admission validation.
#
# NOTE: caBundle is intentionally left empty here. It is patched at runtime by
# the in-cluster controller (WebhookCertReconciler) using the self-signed CA
# generated by pkg/webhook.CertManager. Do not pre-populate caBundle; it would
# be overwritten on the next reconcile cycle anyway.
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: rbgs-validating-webhook-configuration
  labels:
    control-plane: rbgs-controller
    app.kubernetes.io/name: rbgs
    app.kubernetes.io/managed-by: {{ .Release.Service }}
webhooks:
- name: vrolebasedgroup.kb.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  failurePolicy: Fail
  clientConfig:
    service:
      name: rbgs-webhook-service
      namespace: {{ .Release.Namespace }}
      path: /validate-workloads-x-k8s-io-v1alpha2-rolebasedgroup
  rules:
  - apiGroups:
    - workloads.x-k8s.io
    apiVersions:
    - v1alpha2
    operations:
    - CREATE
    - UPDATE
    resources:
    - rolebasedgroups
---
# MutatingWebhookConfiguration for RoleBasedGroup defaulting.
#
# NOTE: caBundle is intentionally left empty here, same as the validating
# configuration above. WebhookCertReconciler patches it at runtime.
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: rbgs-mutating-webhook-configuration
  labels:
    control-plane: rbgs-controller
    app.kubernetes.io/name: rbgs
    app.kubernetes.io/managed-by: {{ .Release.Service }}
webhooks:
- name: mrolebasedgroup.kb.io
  admissionReviewVersions:
  - v1
  sideEffects: None
  failurePolicy: Fail
  clientConfig:
    service:
      name: rbgs-webhook-service
      namespace: {{ .Release.Namespace }}
      path: /mutate-workloads-x-k8s-io-v1alpha2-rolebasedgroup
  rules:
  - apiGroups:
    - workloads.x-k8s.io
    apiVersions:
    - v1alpha2
    operations:
    - CREATE
    - UPDATE
    resources:
    - rolebasedgroups
